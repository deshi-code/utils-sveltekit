 import { describe, expect, it } from 'vitest';
 import PreDebug from './PreDebug.svelte';

describe('PreDebug.svelte', () => { 
    // Tests and new things later
    it('should be truthy', () => {
        expect(PreDebug).toBeTruthy();
    });
});