 import { describe, expect, it } from 'vitest';
 import PreDebug from '../PreDebug/PreDebug.svelte';

describe('DevDeviceBlock.svelte', () => { 
    // Tests and new things later
    it('should be truthy', () => {
        expect(PreDebug).toBeTruthy();
    });
});